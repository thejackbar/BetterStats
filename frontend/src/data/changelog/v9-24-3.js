export default {
  version: 'v9.24.3',
  date: '2026-08-15',
  sortKey: '2026-08-17T19:00:00Z',
  title: 'Sales Workspace — bulk assignment',
  items: [
    'Super admins can now select several clubs in the Workspace queue at once (checkboxes, plus a "Select all filtered" toggle) and assign them in one action — filter down to a batch first (Never called, Callback due, a stage), tick the reps who should take them, and either assign the whole selection to one person or split it evenly between several, round-robin.',
    'Every assignment made this way leaves the same reassignment record on the club\'s own activity history as assigning one at a time, so nothing about the audit trail changes — just how many clubs move in one click.',
  ],
}
